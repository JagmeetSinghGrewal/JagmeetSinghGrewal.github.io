import React from "react"
import styled from '@emotion/styled'
import { Card } from '@mui/material';

const CardStyling = {
  padding: "32px",
  backgroundColor: "cyan",
  borderRadius: "10px",
  borderStyle: "groove"
}


const Repo = () =>  {
    //console.log(this.props.data)
    return (
        //<Card style={CardStyling}> {this.props.data.name} </Card>
        ''
    )
  
}

export default Repo